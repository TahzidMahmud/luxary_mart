import React from 'react';

const Spinner = () => {
    return (
        <div className="w-4 aspect-square border-4 border-white/60 border-l-white rounded-full animate-spin"></div>
    );
};

export default Spinner;
